// default mean we can import landing as default only 1 function no other things this is used for lazy loading
// means giving simply main need, by default it gives this as default
export default function Dashboard() {
    return <div>
        Dashboard Page
    </div>
}