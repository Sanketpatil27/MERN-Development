// constatnt array means arr- constant not values in it, we can modify values in arr
const arr = [1, 2, 3, 4, 5, 6, 7];

// we can push & pop in constant array
arr.push(23,32);
arr.pop();

// but can't assign new array to arr
// arr = [328, 543];       // give erorr 

console.log(arr);
